export interface generateOrderProps {
    products: any;
    total_price: any;
    custId: any;
    addrId: any;
}

export interface updateOrderProps { 
    id: any; 
    update: any; 
    products: any; 
    addrId: any; 
    total_price: any; 
}